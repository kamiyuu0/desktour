<div class="max-w-6xl mx-auto p-6">
    <h1 class="text-2xl font-bold text-gray-800 mb-6">投稿一覧</h1>
    <div class="grid grid-cols-2 gap-4 w-full">
        <% if @desks.present? %>
          <%= render @desks %>
        <% else %>
          <div class="mb-3">投稿されたデスクがありません</div>
        <% end %>
    </div>
</div>